<div>
    <h3>Tiempo restante: <span id="timer"></span></h3>
</div>
<script>
    // setear el timer (10 minutos = 600 segundos)
    let secondsLeft = @(ViewBag.tiempoRestante ?? 600);

    function updateTimer() {
        let minutes = Math.floor(secondsLeft / 60);
        let seconds = Math.floor(secondsLeft % 60);
        document.getElementById('timer').textContent =
            minutes.toString().padStart(2, '0') + ':' +
            seconds.toString().padStart(2, '0');
        if (secondsLeft > 0) {
            secondsLeft--;
            setTimeout(updateTimer, 1000);
        } else {
            window.location.href = '@Url.Action("TiempoTerminado", "Home")';
        }
    }
    updateTimer();
</script>

<section class="MensajeAyudante">
<img src="" alt="">
<h2>Han demostrado su astucia al entrar en la oficina de Dami Asman, pero el juego no ha terminado. Aquí, en su propio espacio, Asman ha dejado el filtro final para los que buscan el legado de Wolfus, Acquistapace y Daiksel. Él, un hombre que mira hacia el futuro y las conexiones globales, no esconde sus secretos en cifras, sino en visiones.
</h2>
 <h3>Observen con atención la pared, donde Asman ha colgado recuerdos de sus viajes. Esos pasajes de avión, a primera vista meros souvenirs, son mucho más que eso.</h3>
</section>

<img src="" alt=""> (imagen de los tickets x IA, lo hago en mi casa con chat gpt plus.)


<form action='@Url.Action("jugarSala", "Home")' method="post">
    <label for="respuestas">Ingresá tu respuesta:</label>
    <input type="text" name="respuestas" id="respuestas" required />
    <input type="submit" value="Enviar respuesta" />
</form>
