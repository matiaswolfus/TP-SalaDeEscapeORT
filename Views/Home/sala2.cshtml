<body class="SALA2">

<div>
    <h3>Tiempo restante: <span id="timer"></span></h3>
</div>
<script>
    // Set the timer (default to 60 seconds if ViewBag.tiempoRestante is null)
    let secondsLeft = @Html.Raw(ViewBag.tiempoRestante ?? 600);

    function updateTimer() {
        let minutes = Math.floor(secondsLeft / 60);
        let seconds = Math.floor(secondsLeft % 60);
        document.getElementById('timer').textContent =
            minutes.toString().padStart(2, '0') + ':' +
            seconds.toString().padStart(2, '0');
        if (secondsLeft > 0) {
            secondsLeft--;
            setTimeout(updateTimer, 1000);
        } else {
            // Ensure the URL is properly rendered as a string
            window.location.href = "@Url.Action("perdiste", "Home")";
        }
    }
    updateTimer();
</script>

<section class="MensajeAyudante">

<img src="" alt="">

</section>

<section>
    <img class="MensajeAyudante" src="/Imagenes/ChicoFeliz.png" alt="">
    <div class="mostrar">
    <a href="#">Ver misión ⬇️</a>
        <div class="hiden">
            <h2>Que poetico! Pero no entiendo NADA!. Ayudenme a realizar esta adivinanza. ¿Como que una LISTA VITAL?, ¿Fundamental?</h2>
            <h3>Corran!, no queda tiempo, ahi vienen los del gestion!. Rapido!! </h3>

            <section class = "EstiloPoema">
<p>En este laberinto de datos <br>
 donde no se puede comer ni tomar, <br> 
				    	  en algún lugar del labo <br>
 hay un secreto que encontrar <br>
<br>
Recuerden el inicio <br>
 la foto que la entrada mostró. <br>
 En su borde olvidado <br>
lo que Asman pide se asomó. <br>
<br>
No es el centro, ni la vista principal <br>
pero es la base de todo, lo fundamental <br>
 ¿Qué lista vital, aunque asi no lo interprete, <br>
 guarda las normas para que Asman no te rete? <br>
</p>
</section>
        </div>
</section>




<form action='@Url.Action("jugarSala", "Home")' method="post">
    <label for="respuestas">Ingresá tu respuesta:</label>
    <input type="text" name="respuestas" id="respuestas" required />
    <input type="submit" value="Enviar respuesta" />
</form>
</main>
    
</body>