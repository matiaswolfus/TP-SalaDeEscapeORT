<header>
    <h1>Sala 2 </h1>
</header>

<main>
    <div>
    <h3>Tiempo restante: <span id="timer"></span></h3>
</div>
<script>
    // setear el timer (10 minutos = 600 segundos)
    let secondsLeft = @(ViewBag.tiempoRestante ?? 600);

    function updateTimer() {
        let minutes = Math.floor(secondsLeft / 60);
        let seconds = Math.floor(secondsLeft % 60);
        document.getElementById('timer').textContent =
            minutes.toString().padStart(2, '0') + ':' +
            seconds.toString().padStart(2, '0');
        if (secondsLeft > 0) {
            secondsLeft--;
            setTimeout(updateTimer, 1000);
        } else {
            window.location.href = '@Url.Action("TiempoTerminado", "Home")';
        }
    }
    updateTimer();
</script>

<section class="MensajeAyudante">

<img src="" alt="">
<h2>Que poetico! Pero no entiendo NADA!. Ayudenme a realizar esta adivinanza. ¿Como que una LISTA VITAL?, ¿Fundamental?
 </h2>
 <h3>Corran!, no queda tiempo, ahi vienen los del gestion!. Rapido!! </h3>
</section>

<section class = "EstiloPoema">
<p>En este laberinto de datos
 donde no se puede comer ni tomar, 
				    	  en algún lugar del labo
 hay un secreto que encontrar

Recuerden el inicio
 la foto que la entrada mostró.
 En su borde olvidado
lo que Asman pide se asomó.

No es el centro, ni la vista principal
pero es la base de todo, lo fundamental
 ¿Qué lista vital, aunque asi no lo interprete,
 guarda las normas para que Asman no te rete?
</p>
</section>


<form action='@Url.Action("jugarSala", "Home")' method="post">
    <label for="respuestas">Ingresá tu respuesta:</label>
    <input type="text" name="respuestas" id="respuestas" required />
    <input type="submit" value="Enviar respuesta" />
</form>
</main>